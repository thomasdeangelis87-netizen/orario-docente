{
  "name": "orario-docente-v16-2",
  "private": true,
  "version": "16.2.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build:identity": "esbuild src/identity-client.js --bundle --format=esm --platform=browser --target=es2022 --outfile=identity-client.js"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0",
    "@netlify/identity": "^2.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
